apply plugin: "java-library"
apply plugin: "kotlin"

dependencies {
    api project(":swap:api")
}

dependencies {
    implementation libraries.rxKotlin
}

dependencies {
    testImplementation libraries.junit4
    testImplementation libraries.assertj
}